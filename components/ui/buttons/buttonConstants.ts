import { colors } from '../../../styles/theme';

// buttonConstants.ts: Constants for the button colors.
export const BUTTON_COLOR_MAP = {
  primary: colors.greenMedium,
  secondary: colors.secondary,
  danger: colors.red,
};
